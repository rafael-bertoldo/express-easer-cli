import {Request, Response, NextFunction} from 'express'

export const create<%= props.name %>Service = async () => {
  const body = req.body

  const new<%= props.name %> = await create<%= props.name %>Service
}

export const read<%= props.name %>Service = async () => {
  const body = req.body

  const new<%= props.name %> = await read<%= props.name %>Service
}

export const update<%= props.name %>Service = async upate() => {
  const body = req.body

  const new<%= props.name %> = await update<%= props.name %>Service
}

export const delete<%= props.name %>Service = async () => {
  const body = req.body

  const new<%= props.name %> = await delete<%= props.name %>Service
}