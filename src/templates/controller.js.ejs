import {Request, Response, NextFunction} from 'express'

export const create<%= props.name %>Controller = async (req: Request, res: Response, next: NextFunction) => {
    const body = req.body

    const new<%= props.name %> = await create<%= props.name %>Service
}

export const read<%= props.name %>Controller = async (req: Request, res: Response, next: NextFunction) => {
    const body = req.body

    const new<%= props.name %> = await read<%= props.name %>Service
}

export const update<%= props.name %>Controller = async (req: Request, res: Response, next: NextFunction) => {
    const body = req.body

    const new<%= props.name %> = await update<%= props.name %>Service
}

export const delete<%= props.name %>Controller = async (req: Request, res: Response, next: NextFunction) => {
    const body = req.body

    const new<%= props.name %> = await delete<%= props.name %>Service
}